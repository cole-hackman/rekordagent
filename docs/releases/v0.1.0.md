# v0.1.0 Release Notes

> Ready for manual library verification and final v0.1.0 tagging.

## Summary

Foundation MVP release for read-only Rekordbox 7 library browsing, intelligent agent auditing, and XML metadata export.

## Included

- Read-only SQLCipher access to Rekordbox `master.db`.
- Track table with filter/sort and "Global Library" layout.
- Track detail panel with clean metadata displays and hot cues.
- Native audio preview play/pause with placeholder visual representation.
- Settings for theme, library path, and Anthropic API key.
- Claude chat interface with intelligent orchestration to propose changes safely.
- Agent read-only MVP tools: search, get track, list playlists, get playlist, list cues, orphan scan, duplicate scan, broken metadata scan.
- Conversation persistence in local SQLite WAL cache.
- Safe staged changes system (`Proposed`, `Accepted`, `Rejected`, `Exported`).
- Grouped inline diff review UI with interactive status filters.
- Export accepted changes to Rekordbox XML with full support for playlist mutations (Rename, Delete, Create, Add/Remove Track).
- Automated test suites (Rust backend, React frontend unit tests, Playwright E2E).

## Known Limitations

- Synthetic decorative waveform inside the chat preview is still labelled "preview". Real Pioneer ANLZ color/detail waveforms (PWAV/PWV3/PWV4/PWV5) *are* rendered in the track inspector via the native ANLZ parser.
- The agent currently stages metadata and playlist modifications but cannot write to `master.db` natively; changes must be exported to XML and manually re-imported into Rekordbox.
- Embedded chat still uses the Anthropic API key path (ADR-0002). Subscription-backed Claude users should use `decks mcp` from Claude Code as the model host.
- POPM rating field is not yet read from ID3 (`crates/audio-tags/src/lib.rs:142`); deferred per DECISIONS.md.

## Build Artefacts (2026-05-15)

- `target/release/bundle/macos/decks.app`
- `target/release/bundle/dmg/decks_0.1.0_aarch64.dmg`

Both unsigned/un-notarised. Signing + notarisation are a future CI concern.

## Release Gate

- [x] Full automated suite passes (`cargo test --workspace` 479, `pnpm test` 126, `pnpm e2e` 4, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `cargo clippy --workspace --all-targets -- -D warnings`).
- [x] `pnpm --filter desktop tauri build` produces fresh `.app` / `.dmg`.
- [ ] `docs/MANUAL_TEST_PLAN.md` v0.1.0 checklist passes against a real Rekordbox 7 library.
- [ ] Tag `v0.1.0` created and pushed.
